package com.labmentix.docsign.notification.entity;

public enum NotificationType {
    SIGNING_INVITE,
    DOCUMENT_SIGNED,
    DOCUMENT_COMPLETED,
    REMINDER,
    DOCUMENT_EXPIRED,
    DOCUMENT_CANCELLED
}